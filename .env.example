# Azure App Service Environment Variables Configuration
# Copy this file to .env for local development
# In Azure App Service, configure these as Application Settings

# Node Environment
NODE_ENV=production

# Server Configuration
PORT=8080
# Azure App Service automatically sets PORT, but default to 8080 for compatibility

# Database Configuration (SQLite)
DATABASE_PATH=./data/sqlite.db
# Optional: Specify custom SQLite database file location
# Default: ./data/sqlite.db
# For Azure App Service: Use /home/data/sqlite.db for persistent storage
# Note: Database file is created automatically if it doesn't exist

# GitHub OAuth Configuration (Recommended - for user authentication)
GITHUB_CLIENT_ID=your_github_oauth_app_client_id
GITHUB_CLIENT_SECRET=your_github_oauth_app_client_secret
GITHUB_CALLBACK_URL=http://localhost:5000/api/auth/github/callback
# For Azure production: https://your-app.azurewebsites.net/api/auth/github/callback
#
# Setup instructions:
# 1. Go to GitHub Settings → Developer settings → OAuth Apps → New OAuth App
# 2. Application name: "GitHubSpark"
# 3. Homepage URL: http://localhost:5000 (or your production URL)
# 4. Authorization callback URL: (use callback URL above)
# 5. Copy Client ID and Client Secret here

# GitHub API Token (Optional - fallback for unauthenticated users)
GITHUB_TOKEN=ghp_your_personal_access_token_here
# Only needed if you want to allow anonymous users to analyze repositories
# When users login with GitHub OAuth, they use their own tokens
# Create a GitHub Personal Access Token (PAT) at: https://github.com/settings/tokens
# Required scopes: repo, read:user, read:org

# Session Configuration (Required)
SESSION_SECRET=your-secure-random-session-secret-here
# Generate a secure random string: openssl rand -base64 32
# IMPORTANT: Use a strong secret in production!

# Optional: Application Insights (Azure monitoring)
# APPLICATIONINSIGHTS_CONNECTION_STRING=InstrumentationKey=your-key-here;IngestionEndpoint=https://...
